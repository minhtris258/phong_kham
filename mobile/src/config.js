// src/config.js
// Thay đổi IP máy tính của bạn tại đây
const IP_ADDRESS = "192.168.1.4"; 
const PORT = "3000";

export const BASE_URL = `http://${IP_ADDRESS}:${PORT}`;
export const API_URL = `http://${IP_ADDRESS}:${PORT}/api`;